# Домашнее задание: MCP-сервер для работы с базой данных — план этапов и статус

**Цель:** MCP-сервер, дающий AI-агенту read-only доступ к SQLite-базе интернет-магазина
(`customers` → `orders` → `order_items` → `products`), подключаемый через stdio.

**Ограничение задания:** код MCP-сервера пишется не вручную, а AI coding agent'ом.

---

## Легенда

- `[x]` — сделано и подтверждено (тестом, файлом или запуском)
- `[ ]` — не сделано
- `[~]` — сделано частично / с оговоркой (пояснение рядом)

**Сводка проверки от 2026-08-23:** реализовано на Node.js, `npm test` → **206/206 passed**.
Docker-образ собран и проверен живым MCP-клиентом. По содержанию задания незакрытых пунктов нет.

⚠️ **Не закоммичено.** Дополнение данных и всё, что из него следует, сейчас лежит только в
рабочей копии: `git status` показывает 21 изменённый файл, новый каталог `scripts/` и новый
[test/data-invariants.test.js](test/data-invariants.test.js), а
`origin/master` стоит на `2ccd1ef` — до сидирования. Клон `origin/master` сегодня получит
исходную базу (150/750/1900), без `country`, без скрипта, с README, который об этом не знает,
и с **красным** прогоном тестов. Коммит и push — за владельцем репозитория.

**Обновление данных.** В исходной базе не было ни колонки страны, ни заказов за 2025 год,
поэтому задачи 2, 3 и 7 отвечались честным «в данных этого нет». Владелец решил дополнить
данные, и теперь отвечаются **все восемь** вопросов. Дополнение сделано одним
детерминированным скриптом [scripts/seed-extended-data.mjs](scripts/seed-extended-data.mjs)
и явно помечено как сгенерированное — в README есть таблица «какие строки предоставлены, а
какие сгенерированы». Ни одна исходная строка не изменена, ни один заказ 2026 года не тронут.

---

## Этап 0. Подготовка

- [x] 0.1 Выбран стек: **Node.js** (ESM, `node >= 20`) — [package.json](package.json)
- [x] 0.2 База `shop.db` включена в проект — [shop.db](shop.db). Исходно: 150 клиентов, 50 товаров, 750 заказов, 1900 позиций. Сейчас: **235 клиентов, 50 товаров, 960 заказов, 2429 позиций** — разница целиком создана скриптом сидирования
- [x] 0.3 Код написан через AI coding agent (Claude Code + скиллы) — [.claude/skills/](.claude/skills/)
- [x] 0.4 План и разбиение на задачи зафиксированы — [tasks/plan.md](tasks/plan.md), [tasks/todo.md](tasks/todo.md)
- [x] 0.5 Дополнение данных вынесено в коммитнутый воспроизводимый скрипт с фиксированным seed'ом, проверяющий 14 инвариантов на каждом прогоне (`npm run seed`) — [scripts/seed-extended-data.mjs](scripts/seed-extended-data.mjs)

---

## Этап 1. Запуск MCP-сервера (§2 задания)

- [x] 1.1 Сервер работает через **stdio** (`StdioServerTransport`) — [src/index.js:55](src/index.js#L55)
- [x] 1.2 Запускается как `{"command": "node", "args": [".../src/index.js"]}` — [mcp.config.example.json](mcp.config.example.json)
- [x] 1.3 Сервер сам подключается к `shop.db`, отдельный HTTP-сервер не нужен — [src/db.js](src/db.js)
- [x] 1.4 `stdout` защищён от постороннего вывода (`console.log` → `console.error` первым же стейтментом) — [src/index.js:1-6](src/index.js#L1-L6)
- [x] 1.5 Используется официальный MCP SDK (`@modelcontextprotocol/sdk@^1.30.0`)
- [x] 1.6 E2E-тест: реальный handshake клиента с дочерним процессом — [test/server.e2e.test.js](test/server.e2e.test.js)

---

## Этап 2. Знакомство агента со схемой (§3, задача 1)

- [x] 2.1 `list_tables` — таблицы/представления, число строк, сводка колонок — [src/tools.js:29](src/tools.js#L29)
- [x] 2.2 `describe_table` — колонки (тип/NOT NULL/PK/default), foreign keys, CHECK-констрейнты, индексы, DDL, 3 примера строк — [src/introspect.js](src/introspect.js)
- [x] 2.3 Служебные таблицы SQLite (`sqlite_sequence` и т.п.) скрыты
- [x] 2.4 Ошибка на несуществующей таблице возвращает список реальных таблиц
- [x] 2.5 Тесты — [test/introspect.e2e.test.js](test/introspect.e2e.test.js), [test/describe-table.e2e.test.js](test/describe-table.e2e.test.js)

**Задача 1 («Show me all available tables…») — [x] агент отвечает полностью.**

---

## Этап 3. Выполнение SQL-запросов (§3, задачи 2–8)

- [x] 3.1 `run_sql_query` — один read-only `SELECT` / `WITH…SELECT` / `VALUES` — [src/tools.js:88](src/tools.js#L88)
- [x] 3.2 Возврат `{columns, rows, row_count, limit, offset, has_more, notes}` — [src/db.js](src/db.js)
- [x] 3.3 Двойная защита: SQL-guard **плюс** отказ, если `stmt.readonly === false`
- [x] 3.4 Корректная сериализация всех типов SQLite (BLOB, BigInt, NULL, REAL, TEXT) с обрезкой ячеек > 4 KB
- [x] 3.5 Ограничение времени выполнения (`QUERY_TIMEOUT`) для потоковых планов
- [x] 3.6 Тесты — [test/run-sql-query.e2e.test.js](test/run-sql-query.e2e.test.js), [test/db.test.js](test/db.test.js)

### Контрольные вопросы задания

- [x] **Задача 2.** «How many customers are from Germany?» → **24** клиента. Колонка `customers.country` добавлена скриптом сидирования; у каждого сгенерированного клиента имя, код телефона и домен почты согласованы со страной (`Ben Weber`, `+49151…`, `ben.weber143@freenet.de`, `Germany`)
- [x] **Задача 3.** «Which country has the most customers?» → **Россия, 150 из 235**. Второе место Германия (24), третье Франция (16). Ничьих в рейтинге нет, ответ однозначен
- [x] **Задача 4.** Кто потратил больше всех → Екатерина Харитонов, `ekaterina.kharitonov777@gmail.com`, **859 460.00** за 11 заказов (без отменённых; 885 420.00 за 12 с ними). Первая на обеих базах
- [x] **Задача 5.** Топ-5 товаров → возвращаются **и units, и revenue**; по выручке лидер Ноутбук UltraBook 15 (98 шт., 8 819 020.00), по штукам — Планшет Tab 10 (123 шт., 4 303 770.00)
- [x] **Задача 6.** Топ-3 категории по выручке → Электроника 23 011 170.00, Бытовая техника 7 237 660.00, Одежда и обувь 3 736 320.00 (join `orders → order_items → products`; с отменёнными порядок тот же)
- [x] **Задача 7.** Выручка за 2025 → **8 990 280.00** по 183 заказам (10 906 130.00 по 210 с отменёнными). Все заказы 2025 года — сентябрь–декабрь и все сгенерированы: раньше сентября их нельзя было поставить, потому что в предоставленных данных каталог начинается 2025-08-24, а первый клиент зарегистрирован 2025-08-22
- [x] **Задача 8.** Больше всех заказов → София Яковлев, `sofiya.yakovlev284@yandex.ru`, **16** заказов (17 с отменёнными)

> Задачи 2, 3 и 7 закрыты дополнением данных, а не оговоркой. Что именно сгенерировано и
> почему — README → «Known data limitations», раздел 0. Механизмы честного ответа при этом
> никуда не делись: `SELECT city FROM customers` по-прежнему возвращает ошибку со списком
> реальных колонок, а пустой диапазон (например, 2024 год) — реальные границы данных.

---

## Этап 4. Safety: только чтение (§4)

- [x] 4.1 База открывается в режиме `readonly` — [src/db.js](src/db.js)
- [x] 4.2 SQL-guard: allow-list открывающих ключевых слов + правило «ровно один стейтмент» — [src/sql-guard.js](src/sql-guard.js)
- [x] 4.3 Лексер вырезает комментарии (`--`, `/* */`), строковые литералы и кавычки идентификаторов **до** матчинга глаголов — обход через `/* SELECT */ DELETE …` не работает, а `WHERE name = 'DROP TABLE orders'` при этом разрешён
- [x] 4.4 Запрещены `INSERT` / `UPDATE` / `DELETE` / `DROP` / `ALTER` / `CREATE` (включая `CREATE TEMP`)
- [x] 4.5 Запрещены `PRAGMA`, `ATTACH`, `DETACH`, `VACUUM`, `REINDEX`, `BEGIN`/`COMMIT`/`ROLLBACK`/`SAVEPOINT`, `EXPLAIN <write>`
- [x] 4.6 Запрещены записи, спрятанные в CTE (`WITH x AS (...) DELETE …`)
- [x] 4.7 **«Delete all cancelled orders.»** → отказ `READ_ONLY_VIOLATION` + подсказка эквивалентного `SELECT`
- [x] 4.8 Capstone-тест: **45 атакующих запросов + 2 инъекции идентификатора** через живого MCP-клиента, после прогона SHA-256 и размер копии базы **не изменились** — [test/safety.e2e.test.js](test/safety.e2e.test.js)
- [x] 4.9 Сверх исходного списка покрыты: невложенность блочных комментариев (`/* /* */ DELETE … */` SQLite читает как DELETE), незакрытые токены, нулевой байт, `load_extension` (для него `sqlite3_stmt_readonly` возвращает `true`, т.е. проверка на уровне statement его НЕ ловит), `ANALYZE`, BOM, blob-литералы, лимит длины, `pragma_*` как table-valued функции, и три формы `RETURNING`
- [x] 4.10 Проверено, что 9 легитимных запросов **не** блокируются — набор из одних отказов прошёл бы и на сервере, отвергающем всё

---

## Этап 5. Дизайн tools (§5)

- [x] 5.1 Гибридный дизайн: 3 базовых tool'а + 3 специализированных, а не по tool'у на вопрос
- [x] 5.2 Базовые: `list_tables`, `describe_table`, `run_sql_query`
- [x] 5.3 Специализированные: `top_customers_by_spend`, `top_products_by_sales`, `revenue_by_period` — [src/analytics.js](src/analytics.js)
- [x] 5.4 Специализированные tools регистрируются **только если схема их поддерживает** (`probeCapabilities`) — [src/tools.js:143](src/tools.js#L143)
- [x] 5.5 Описания в едином формате **WHEN TO USE / RETURNS / LIMITS**; блок `WHAT TO PASS` добавлен только у `run_sql_query`, где агент передаёт произвольный SQL. Единообразие проверяется структурно тестом, а не на глаз
- [x] 5.6 Аннотации `readOnlyHint: true`, `destructiveHint: false`, `openWorldHint: false` на каждом tool
- [x] 5.7 `instructions` сервера направляют агента: сначала осмотреться, не выдумывать недостающее — [src/index.js:15-31](src/index.js#L15-L31)
- [x] 5.8 `include_cancelled` (по умолчанию `false`) и явный `rank_by` — неоднозначность решает агент, а не сервер
- [x] 5.9 Тест на качество описаний — [test/descriptions.e2e.test.js](test/descriptions.e2e.test.js)

---

## Этап 6. Технические требования (§6)

- [x] 6.1 Используется MCP SDK
- [x] 6.2 Работа через stdio
- [x] 6.3 SQLite (`better-sqlite3`)
- [x] 6.4 Корректная обработка ошибок: **шесть** типизированных кодов — `READ_ONLY_VIOLATION` / `SQL_ERROR` / `INVALID_ARGUMENT` / `QUERY_TIMEOUT` / `CONFIGURATION_ERROR` / `INTERNAL_ERROR` — [src/errors.js](src/errors.js). Неизвестные ошибки становятся `INTERNAL_ERROR`, а не `SQL_ERROR`: иначе внутренний баг сообщал бы агенту, что неверен его запрос, и тот переписывал бы его бесконечно
- [x] 6.5 Stack traces и абсолютные пути не утекают агенту (санитайзер + тест «ни одно сообщение не содержит `/`-пути и `at ` фрейма»)
- [x] 6.6 Ошибки обучающие: `no such column` дополняется списком реальных колонок, `no such table` — списком таблиц
- [x] 6.7 Абсолютных путей в коде нет
- [x] 6.8 Путь к базе — из `SHOP_DB_PATH`, иначе относительно корня проекта через `import.meta.url` (не `process.cwd()`) — [src/config.js](src/config.js)
- [x] 6.9 Недоступная база не роняет сервер: handshake проходит, tools отвечают `CONFIGURATION_ERROR` — [test/boot.e2e.test.js](test/boot.e2e.test.js)
- [x] 6.10 README с цепочкой **install → configure → run → connect to agent** — [README.md](README.md)

---

## Этап 7. Что сдавать (§7)

- [x] 7.1 `README.md` — [README.md](README.md)
- [x] 7.2 `package.json` — [package.json](package.json)
- [x] 7.3 Исходный код — [src/](src/)
- [x] 7.4 Пример конфигурации — [mcp.config.example.json](mcp.config.example.json)
- [x] 7.5 Конфиг для подключения к агенту (Claude Code) — [.mcp.json](.mcp.json)
- [x] 7.6 `shop.db` включён в репозиторий (явно не игнорируется, см. комментарий в [.gitignore](.gitignore)), вместе со скриптом, которым он был дополнен — [scripts/seed-extended-data.mjs](scripts/seed-extended-data.mjs)
- [x] 7.7 Тест, проверяющий, что пример конфигурации реально запускается — [test/config-example.test.js](test/config-example.test.js)
- [~] 7.8 **Закоммичено до сидирования; дополнение данных ещё нет.** История `master` линейная, `origin/master` синхронен с локальным `master` на коммите `2ccd1ef` — `https://github.com/Kirill-Od/larchik_3.git`. Всё, что связано с дополнением данных (обновлённый `shop.db`, `scripts/seed-extended-data.mjs`, перемеренные тесты, README и этот файл), лежит в рабочей копии незакоммиченным. Коммит и push оставлены владельцу

---

## Этап 8. Критерии проверки (§8)

- [x] 8.1 MCP успешно запускается
- [x] 8.2 Агент видит MCP tools
- [x] 8.3 Агент получает информацию о структуре БД
- [x] 8.4 Агент отвечает на аналитические вопросы
- [x] 8.5 Агент корректно выполняет multi-step queries (`orders → order_items → products`)
- [x] 8.6 MCP корректно обрабатывает ошибки
- [x] 8.7 MCP не позволяет изменять базу (подтверждено хешем файла после атак)
- [x] 8.8 Tools имеют понятные descriptions и schemas
- [x] 8.9 Финальная проверка на «живом» агенте выполнена (headless Claude Code через [.mcp.json](.mcp.json)) **и задокументирована** в README → «Verified end to end with a live agent»: агент увидел tools, получил отказ на `DELETE FROM orders WHERE status='cancelled'`, самостоятельно ответил «данных за 2025 нет, выручка равна нулю, **а не неизвестна**» и сам нашёл в данных ничью на 11 заказах. SHA базы до и после совпал. Отдельно задокументирован прогон в контейнере.
  **Прогон сделан до сидирования, на исходной базе**, и две процитированные цифры с тех пор изменились: выручка за 2025 теперь **8 990 280.00**, а на втором месте по числу заказов Полина Петрова с 13, а не ничья на 11. Отказ на `DELETE` и неизменность хеша от дополнения данных не зависят и воспроизводятся сегодня. В README это помечено там же

---

## Этап 9. Bonus (§9)

- [x] 9.1 Pagination больших результатов (`limit` / `offset` / `has_more` + подсказка следующего вызова в `notes`)
- [x] 9.2 Понятная обработка SQL errors (с подстановкой реальных колонок/таблиц)
- [x] 9.3 Ограничение количества строк (по умолчанию 100, максимум 1000) и общего размера ответа (~256 KB)
- [x] 9.4 Специализированные tools для частых операций (3 аналитических)
- [x] 9.5 Хорошие descriptions (единый формат + тест)
- [x] 9.6 Автоматические тесты — **206 тестов, все проходят** (`npm test`), 18 файлов unit + e2e — [test/](test/). После дополнения данных все зафиксированные в тестах эталонные значения перемерены и обновлены, ни одна проверка не ослаблена. Отдельный файл [test/data-invariants.test.js](test/data-invariants.test.js) заново прогоняет все инварианты по закоммиченной базе и проверяет две защиты самого скрипта: откат при сбое посередине и отказ работать с чужой базой
- [x] 9.7 Docker support (multi-stage `node:22-slim`, обе стадии на одном базовом образе ради ABI-совместимости нативного бинарника, `npm ci` с локфайлом, `USER node`, exec-form ENTRYPOINT) — [Dockerfile](Dockerfile). **Образ собран и проверен в работе** на Docker 23.0.5:
  - `docker build -t shop-db-mcp .` → успешно, `sha256:00cf543e8362…`
  - контейнер подключён живым MCP-клиентом командой `docker run -i --rm shop-db-mcp`: видны все 6 tools, `list_tables` отдаёт схему, `SELECT COUNT(*) … WHERE status <> 'cancelled'` вернул **648** (сходится с 750 − 102), `DELETE FROM orders WHERE status='cancelled'` отклонён с `READ_ONLY_VIOLATION` и подсказкой эквивалентного `SELECT`. **Прогон сделан до сидирования**: на текущей базе тот же запрос вернёт **831** (960 − 129), образ придётся пересобрать, чтобы в него попал дополненный `shop.db`
  - **предупреждение про `-t` проверено, а не заявлено**: с `-i` приходит корректный JSON-RPC; с `-it` TTY включает эхо и запрос клиента возвращается ему же обратно — клиент разберёт собственный `initialize` как ответ сервера. Порча кадров воспроизведена
- [x] 9.8 Качественный README: 810 строк — установка, конфигурация, запуск, подключение, описание всех tools, гарантии read-only, происхождение данных (что предоставлено, что сгенерировано), ограничения данных и техники, ответы на все 8 вопросов

---

## Итог

| Раздел | Готово |
|---|---|
| §2 Требования к MCP | 6 / 6 |
| §3 Задачи агента | 8 / 8 |
| §4 Safety | 8 / 8 |
| §5 Дизайн tools | 9 / 9 |
| §6 Технические требования | 10 / 10 |
| §7 Что сдавать | 7 / 8 — не закоммичено |
| §8 Критерии проверки | 9 / 9 |
| §9 Bonus | 8 / 8 |

**По содержанию задания закрыто всё, включая все восемь аналитических вопросов.** Открытым
остаётся один пункт, и он не про код: **дополнение данных не закоммичено и не запушено**
(§7.8). Пока это не сделано, проверяющий, склонировавший `origin/master`, увидит проект в
состоянии до сидирования. Коммит и push — за владельцем репозитория.
